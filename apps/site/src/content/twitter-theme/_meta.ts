import type { MetaRecord } from 'nextra'

const meta: MetaRecord = {
  index: {
    // Already shown via the section item
    display: 'hidden',
  },
  'api-reference': '',
  advanced: '',
}

export default meta
